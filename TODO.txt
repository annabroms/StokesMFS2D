* Improve distribution of particle centers via tree-based code
* Look at eivenvalues for the laplace solves. CONCLUSION: small eigvals still exist but their magnitude is larger and their effect smaller than for Stokes. How do the few large eigvals affect convergence?
* Does Cmap matrix have any low rank structure? -> Sort of! To be investigated more. The tricky bit is that the same compression would be needed for the Cmap_QV and Cmap_FU mappings.
* Implement rotations to avoid computing essentially the same compression multiple times. -> Done for both Laplace and Stokes. Check how accurate the FFT based interpolation is! 
* Test 1-body left-precond
* Speed up mobility solve by checking if direct eval can be made faster -> compiled version not faster, but what is faster is to use matrix instead of summation in loop.
